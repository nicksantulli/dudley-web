{
  "name": "dudley-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "optimize:assets": "node scripts/optimize-assets.mjs",
    "test": "node --test scripts/content-contract.test.mjs && npm run build && node --test scripts/campaign-links.test.mjs && node --test scripts/built-output.test.mjs && node --test scripts/site-integrity.test.mjs",
    "postbuild": "node scripts/gen-image-sitemap.mjs",
    "preview": "astro preview",
    "deploy": "node scripts/deploy-gh-pages.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.0",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
